import AppLayout from '../../core/layout/AppLayout';

const Talent = () => {
	return (
		<AppLayout>
			<div className="text-center text-malawi-white h-screen">You can find talent here</div>
		</AppLayout>
	);
};

export default Talent;
